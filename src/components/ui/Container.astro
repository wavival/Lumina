---
interface Props {
  as?: string;      
  class?: string;
}

const { as = "section", class: cls = "" } = Astro.props;
const Tag = as as any;
---

<Tag class={`container-prose ${cls}`}>
  <slot />
</Tag>